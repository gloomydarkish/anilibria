import stl from './index.module.scss';

export const Banner = () => {
    return (
        <section className={stl.banner}>
            <div className={stl.banner__wrapper}></div>
        </section>
    );
};
